
interface IStrings {
    btnLabel: string;
    inputErrorPattern: any; 
}

const strings: IStrings = {
    btnLabel: 'Log in _ Agenda',
    inputErrorPattern: {
        email: 'Email is not in a correct format',
        text: 'Incorrect number format'
    }
}

export default strings;